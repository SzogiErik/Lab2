#include <stdio.h>
#include <math.h>

int main()
{
    int a, b, c, d;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    scanf("%d", &d);
    double l;
    l = sqrt((a - c) * (a - c) + (b - d) * (b - d));
    printf("%lf", l);

    // get user input

    // calculate the length

    // print the result

    return 0;
}